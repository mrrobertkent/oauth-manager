services:
  auth-service:
    build: ./auth-service
    entrypoint: ["doppler", "run", "--", "node", "dist/index.js"]
    environment:
      - DOPPLER_TOKEN=${DOPPLER_TOKEN}
    volumes:
      - ./auth-service/data:/app/auth-service/data
      # - ./auth-service/data/orgs.json:/app/auth-service/data/orgs.json:ro
    networks:
      - local_net
    ports:
      - "3001:3001"

networks:
  local_net:
    driver: bridge